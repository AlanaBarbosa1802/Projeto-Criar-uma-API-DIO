import react,{Component} from 'react';
import {View, Text, Image} from 'react-native';

export default class Filme extends Comment {
    render(){
        return(
            <View>

                <Text>iqwhebquer</Text>
            </View>
        )

        const styles = StyleSheet.create({
            container:{

            },
            nomeFilme: {
                color:'white',
                fontSize: 18,
                fontWeight: 'bold'
            }
        })
    }
}
